import{Link}from'react-router-dom';export function Header({title,desc,children}){return <div className="pagehead"><div><h1>{title}</h1><p>{desc}</p></div><div>{children}</div></div>}export function Badge({children}){return <span className={'badge '+String(children).toLowerCase().replaceAll(' ','-')}>{children}</span>}export function Panel({children,className=''}){return <section className={'panel '+className}>{children}</section>};export function Search(){return <div className="searchbox">⌕<input placeholder="Search..."/></div>};export function Button({children,secondary=false,onClick,type='button'}){return <button type={type} onClick={onClick} className={secondary?'btn secondary':'btn'}>{children}</button>};export {Link};
